<local:AllViewBase x:Class="MVVMFirma.Views.AllInvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id"/>
                <DataGridTextColumn Header="Number"/>
                <DataGridTextColumn Header="Supplier id"/>
                <DataGridTextColumn Header="Added by employee id"/>
                <DataGridTextColumn Header="Issue date"/>
                <DataGridTextColumn Header="Due date"/>
                <DataGridTextColumn Header="Tax id"/>
                <DataGridTextColumn Header="Duty by country"/>
                <DataGridTextColumn Header="Total amount"/>
                <DataGridTextColumn Header="Payment id"/>
                <DataGridTextColumn Header="Status changed by employee id"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
